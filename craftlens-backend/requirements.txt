# ─────────────────────────────────────────────────────────────
# CraftLens Backend — OPTIMIZED FOR RENDER FREE TIER (512MB RAM)
# Uses API-based services instead of heavy local ML models
# ─────────────────────────────────────────────────────────────

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# Config / settings
pydantic-settings==2.2.1
pydantic==2.7.1
python-dotenv==1.0.1

# ── API clients for ML services ────────────────────────────────
groq>=0.9.0                       # ASR via Whisper API + LLM
google-generativeai>=0.7.0        # Gemini for LLM + embeddings
httpx>=0.27.0                     # REST API calls

# ── Lightweight image processing ──────────────────────────────
Pillow>=10.0.0                    # Basic image operations
aiofiles>=23.2.1                  # Async file I/O
requests>=2.31.0                  # For remove.bg API

# ── Data handling ─────────────────────────────────────────────
pandas>=2.1.0                     # CSV/metadata handling
numpy>=1.26.0                     # Basic array operations

# ── Persistence ───────────────────────────────────────────────
sqlalchemy>=2.0.0
aiosqlite>=0.19.0
