# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# Settings / config
pydantic-settings==2.2.1
pydantic==2.7.1
python-dotenv==1.0.1

# Phase 1 — Transcription
openai-whisper==20231117
torch==2.3.0          # CPU-only is fine for 'base'/'small' model

# Phase 1 — LLM (Groq free tier, no billing required)
groq==0.9.0

# Phase 3 — Image pipeline
rembg[gpu]==2.0.56    # falls back to CPU automatically; uses isnet-general-use
opencv-python-headless==4.9.0.80
Pillow==10.3.0

# Phase 4 — Pricing / embeddings
sentence-transformers==2.7.0
faiss-cpu==1.8.0
pandas==2.2.2
numpy==1.26.4

# Phase 5 — CLIP visual tags
open-clip-torch==2.24.0

# Phase 7 — Persistence
sqlalchemy==2.0.30
aiosqlite==0.20.0

# Utilities
httpx==0.27.0
aiofiles==23.2.1
